# AI 学社 · 后续完善计划（Frontend Roadmap）

> 评审视角：前端工程架构分析专家
> 评审基准：基于 `ai-academy/` 当前实际代码（13 文件 / 2602 行，纯静态多页站点）
> 制定日期：2026-08-03

---

## 〇、当前状态基线

### 1. 已实现核心能力

| 模块 | 落地情况 | 关键文件 |
|------|----------|----------|
| 多页导航 | 5 个页面（首页/课程/详情/社区/关于），真实跳转 | `index.html` `courses.html` `course-detail.html` `community.html` `about.html` |
| 共享布局 | JS 动态注入 Header/Footer/返回顶部/跳转链接 | `js/layout.js` |
| 设计令牌 | 完整令牌体系（字号/间距/字重/z-index/颜色语义） | `css/tokens.css` |
| 统一图标 | 30+ 内联 SVG 线条图标，替换 emoji | `js/icons.js` |
| 暗色模式 | `[data-theme="dark"]` + 主题切换持久化 | `css/tokens.css` `js/layout.js` |
| 交互层 | 汉堡菜单、滚动渐入、数字动画、平滑滚动 | `js/main.js` |
| 无障碍基础 | `<main>` 地标、skip link、`:focus-visible`、aria | `css/base.css` |

### 2. 五维度架构评分（基线）

| 维度 | 得分 | 评级 | 诊断 |
|------|------|------|------|
| 技术栈健康度 | 34/50 | ⭐⭐⭐ | 纯原生无构建无依赖，稳定但无工程化，版本/依赖无法收敛 |
| 架构设计模式 | 38/50 | ⭐⭐⭐⭐ | 令牌化+模块化 CSS 优秀，但布局靠运行时注入、数据硬编码在 HTML |
| 工程化成熟度 | 18/50 | ⭐⭐ | 无 package.json、无构建、无测试、无 lint、无 CI——**最大短板** |
| 性能与可维护性 | 35/50 | ⭐⭐⭐ | 无打包压缩、无缓存策略、无错误兜底，维护靠手工 |
| 综合 | 31/50 | ⭐⭐⭐ | 原型级作品，距"工程化产品"差一层基建 |

**综合结论**：项目完成度在「作品集 demo」层级已有不错表现，但**工程化地基（构建/测试/配置/容错）为零**。下一阶段核心不是加功能，而是把"能跑的 demo"升级为"可维护、可测试、可部署的工程"。

### 3. 待补齐模块清单

- [ ] 构建工具与依赖管理（Vite + pnpm）
- [ ] 配置管理中心（站点配置 + 课程数据抽离）
- [ ] 全局错误处理与兜底页（404 / 500 / 注入失败）
- [ ] 客户端日志模块
- [ ] 单元测试（Vitest）与覆盖率
- [ ] 表单交互（注册/登录/联系）与校验、Toast 反馈
- [ ] 加载态/骨架屏/空状态
- [ ] Lighthouse 性能与无障碍达标

---

## 一、阶段划分总览

| 阶段 | 主题 | 优先级 | 目标产出 | 预估工作量 |
|------|------|--------|----------|------------|
| 阶段 1 | 工程化地基 | 🔴 P0 | 可构建、可配置、可容错 | 2–3 天 |
| 阶段 2 | 质量保障 | 🟠 P1 | 测试覆盖 + 错误页 | 2 天 |
| 阶段 3 | 交互与体验 | 🟠 P1 | 表单/反馈/加载态 | 2–3 天 |
| 阶段 4 | 功能动态化 | 🟡 P2 | 数据驱动 + 注册登录 | 3–4 天 |
| 阶段 5 | 部署与优化 | 🟢 P3 | CI/CD + 性能达标 | 2 天 |

---

## 二、阶段 1 · 工程化地基（P0）

> **目标**：让项目从"双击打开 HTML"进化为"有构建、有依赖、有配置、有容错"的标准前端工程。

### 任务清单

| # | 任务 | 具体动作 | 关联现状 |
|---|------|----------|----------|
| 1.1 | 初始化工程 | 新增 `package.json`，引入 `pnpm` + `vite`；配置 `dev`/`build`/`preview` 脚本 | 当前无 package.json |
| 1.2 | 配置管理 | 新增 `src/config/site.config.js`（站点名/导航/社交链接）+ `src/config/courses.data.js`（课程数据），从 HTML 硬编码抽离 | 课程数据现散落 `courses.html`/`course-detail.html` |
| 1.3 | 全局错误处理 | `js/main.js` 顶层 `window.onerror` / `unhandledrejection` 兜底；`layout.js` 注入失败时渲染降级 Header | 注入失败会白屏且无提示 |
| 1.4 | 客户端日志 | 新增 `js/logger.js`（level: debug/info/warn/error，dev 输出 console，prod 可上报），替换散落 `console.log` | 无统一日志 |
| 1.5 | 资源路径治理 | 统一为相对路径/Vite 别名 `@/`，消除 4 层 `.workbuddy` 路径脆弱性 | 现路径依赖绝对目录 |

### 验收标准

- [ ] `pnpm install && pnpm build` 成功产出 `dist/`，含带 content-hash 的 CSS/JS
- [ ] `pnpm dev` 启动本地服务，5 页正常渲染，无控制台 error
- [ ] 修改 `site.config.js` 一处，全站导航/品牌同步更新（单源真理）
- [ ] 人为抛错时页面不白屏，显示兜底提示且 `logger` 记录到 console
- [ ] 构建产物 gzip 后：CSS < 30KB，JS < 45KB

### 预期成果

工程从"文件堆"变为"标准 npm 工程"，后续所有改动有构建校验兜底，配置一处定义全局生效。

---

## 三、阶段 2 · 质量保障（P1）

> **目标**：建立可量化的质量防线，让重构与迭代不破窗。

### 任务清单

| # | 任务 | 具体动作 |
|---|------|----------|
| 2.1 | 单元测试 | 引入 `vitest`，对 `icons.js`（图标渲染）、`logger.js`（级别过滤）、`courses.data.js`（数据完整性）、`layout.js`（注入逻辑 mock DOM）编写用例 |
| 2.2 | 静态检查 | 接入 `ESLint`（airbnb-base 或 flat config）+ `Prettier` + `husky` pre-commit 校验 |
| 2.3 | 错误页 | 新增 `404.html`（路由未命中）、`error.html`（运行时兜底），接入 `layout.js` |
| 2.4 | 类型辅助 | 为 `config` / `logger` 关键模块补 JSDoc + 可选 `tsc --noEmit` 轻量类型检查 |

### 验收标准

- [ ] Vitest 用例通过率 100%，核心工具模块行覆盖率 ≥ 80%
- [ ] `pnpm lint` 零 error；pre-commit 自动格式化通过
- [ ] 访问不存在路由显示 404 页而非空白
- [ ] 修改被测试保护的模块时，CI 能拦截明显回归

### 预期成果

具备"改坏会被测出来"的安全网，简历可写"有单元测试与 lint 门禁"。

---

## 四、阶段 3 · 交互与体验（P1）

> **目标**：把"能看"升级为"好用"，补齐用户操作闭环与状态反馈。

### 任务清单

| # | 任务 | 具体动作 | 关联现状 |
|---|------|----------|----------|
| 3.1 | 表单与校验 | 新增注册/登录/联系表单，前端校验（必填/邮箱/密码强度），提交走 `logger` + 模拟异步 | 当前所有 CTA 为 `#` 死链 |
| 3.2 | Toast 反馈 | 新增 `js/toast.js` 轻量通知组件，成功/失败统一提示 | 无操作反馈 |
| 3.3 | 加载/骨架/空态 | 列表与详情页增加 skeleton 占位、空数据状态、按钮 loading 态 | 无异步态概念 |
| 3.4 | 主题增强 | 主题切换增加"跟随系统"（`prefers-color-scheme`）与过渡动画 | 仅手动切换 |
| 3.5 | 可访问性补强 | 表单 `label`/`aria-describedby`、焦点陷阱（移动菜单）、`prefers-reduced-motion` 全覆盖 | 部分覆盖 |

### 验收标准

- [ ] 点击注册/联系按钮出现校验提示与 Toast，非法输入被拦截
- [ ] 模拟网络延迟时显示 skeleton/loading，完成后正常渲染
- [ ] 键盘 Tab 可遍历所有交互元素，焦点可见
- [ ] Lighthouse Accessibility ≥ 95

### 预期成果

用户操作有完整闭环与反馈，体验从"展示页"逼近"应用"。

---

## 五、阶段 4 · 功能动态化（P2）

> **目标**：数据驱动页面，为后续接入后端铺路。

### 任务清单

| # | 任务 | 具体动作 |
|---|------|----------|
| 4.1 | 课程列表动态渲染 | `courses.html` 由 `courses.data.js` 循环生成卡片，支持难度/关键词筛选 |
| 4.2 | 详情页参数化 | `course-detail.html` 读 `?id=` 渲染对应课程，无 id 跳 404 |
| 4.3 | 注册/登录流程 | 纯前端实现注册（localStorage 存用户）、登录态持久化、登出 |
| 4.4 | 搜索与筛选 | 课程/社区增加客户端搜索框，复用 `config` 数据 |

### 验收标准

- [ ] 新增一门课只需在 `courses.data.js` 加一条，列表/筛选/详情自动生效
- [ ] 直接访问 `course-detail.html` 无参数 → 跳转 404
- [ ] 注册后刷新仍保持登录态；登出清除
- [ ] 搜索关键词实时过滤结果

### 预期成果

页面与数据解耦，结构已具备对接 REST API 的形态（后续仅替换数据来源）。

---

## 六、阶段 5 · 部署与优化（P3）

> **目标**：可上线、可度量、可持续。

### 任务清单

| # | 任务 | 具体动作 |
|---|------|----------|
| 5.1 | CI/CD | GitHub Actions：install → lint → test → build → 部署预览 |
| 5.2 | 性能达标 | 图片懒加载、关键 CSS 内联、资源 preload、gzip/brotli |
| 5.3 | SEO 完善 | `sitemap.xml` + `robots.txt` + JSON-LD 结构化数据 |
| 5.4 | 监控（可选） | 生产环境错误经 `logger` 上报到轻量收集端 |

### 验收标准

- [ ] 推送主分支自动跑通 CI，产物可预览
- [ ] Lighthouse Performance ≥ 90，Best Practices ≥ 90
- [ ] 搜索引擎可抓取 sitemap，富结果可解析

### 预期成果

项目达到"可公网部署 + 可观测"的产品级标准。

---

## 七、执行顺序与里程碑

```
Week 1  → 阶段1（地基）+ 阶段2（测试）      → 里程碑 M1：可构建、有测试、有容错
Week 2  → 阶段3（交互）+ 阶段4（动态化）     → 里程碑 M2：可操作、数据驱动
Week 3  → 阶段5（部署/优化）                 → 里程碑 M3：可上线、可度量
```

**M1 是硬门槛**：没有构建与测试，后续所有功能迭代都会累积技术债。建议优先打透阶段 1+2。

---

## 八、风险提示

1. **不要过早引入框架**：当前原生方案对学习者价值最高，Vue/React 可放在阶段 5 之后作为"重构对比"练习，而非现在。
2. **配置与数据边界**：`site.config.js`（静态配置）与 `courses.data.js`（业务数据）必须分离，避免后期耦合。
3. **测试范围克制**：先测纯逻辑模块（icons/logger/config），DOM 注入测试用 jsdom mock，不要为了覆盖率测 UI 细节。

---

> 本报告基于静态分析与项目实际代码生成，仅供参考。架构没有银弹，合适的才是最好的——请结合你的学习节奏与求职目标灵活取舍阶段顺序。
